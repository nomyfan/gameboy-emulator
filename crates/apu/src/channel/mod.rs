mod noise_channel;
mod pulse_channel;
mod volume_envelope;
mod wave_channel;

pub(crate) use noise_channel::NoiseChannel;
pub(crate) use pulse_channel::PulseChannel;
use volume_envelope::VolumeEnvelope;
pub(crate) use wave_channel::WaveChannel;
